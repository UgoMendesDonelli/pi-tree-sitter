// A simple Calculator class
class Calculator {
  // Adds two numbers
  add(a, b) {
    return a + b;
  }

  subtract(a, b) {
    return a - b;
  }
}

function multiply(x, y) {
  return x * y;
}

const PI = 3.14159;
var oldStyle = true;
let mutable = "hello";

const { readFile } = require("fs");
